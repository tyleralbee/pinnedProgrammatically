# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'pinnedProgrammatically' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for pinnedProgrammatically
  pod 'Firebase/Auth'
  pod 'Firebase/Firestore'
  pod 'Firebase/Core'

  pod 'Firebase/Storage'
  pod 'Firebase/Database'
  pod 'FirebaseUI'

  target 'pinnedProgrammaticallyTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'pinnedProgrammaticallyUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
